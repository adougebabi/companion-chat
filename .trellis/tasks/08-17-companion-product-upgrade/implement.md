# Integration Plan

1. Review and approve both child design and implementation plans.
2. Implement and validate the life-domain persistence/API contracts on a fresh database.
3. Implement the Telegram-style interface and integrate only approved domain projections.
4. Perform a cross-task full-scope check: responsive UI, persona isolation, job recovery, paging, hidden/screened visibility, and no debug/prompt disclosure.
5. Update project specs and README for the new same-file SQLite table model, local clean-start behavior, and verification commands.

The parent task is not an implementation target. It closes only when both child tasks independently meet their acceptance criteria and the integration check passes.
