# Provider configuration source

Read-only inspection of the existing dedicated fluctlight-phase8-regression stack found its current cognitive_assessment provider endpoint and model. Reused test settings in private /tmp session environment, no values logged/committed. Endpoint differs from .env and /models probe returned HTTP 200. No secret row is configured for this endpoint. All new business tests use a separate disposable PostgreSQL project and per-test temporary databases. No write/migration on existing regression stack.
