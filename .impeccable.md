## Design Context

### Users

个人用户在桌面和手机上长期使用本地 AI 伴侣。核心工作是与独立人格私聊、查看她们各自的生活动态，并在需要时创建或管理人格。界面应支持频繁、低摩擦的重复操作，而不是一次性的演示。

### Brand Personality

亲近、安静、可信。产品应让人格的生活显得自然可读，但不把用户置于养成游戏或日程监控的压力中。

### Aesthetic Direction

采用明亮、克制的 Telegram 式消息界面：紧凑联系人栏、主导的聊天/动态阅读面、低噪声蓝色操作强调和微暖的中性色。第一版只提供亮色模式；不使用营销式首屏、深色霓虹、紫色渐变或装饰性卡片堆叠。

### Design Principles

1. 聊天与全局动态是并列的一等入口；人格详情承载私有 Moments、状态和治理信息。
2. 先展示当前可行动的信息，再渐进披露人格设定、记忆、事件和调试细节。
3. 移动端以一个完整工作面为单位切换，底部导航保持可达性，文本和触控目标不得溢出。
4. 自动事件、图片队列和屏蔽状态必须具有清晰但不打扰人的反馈。
5. 每位人格的颜色仅作为识别线索，页面整体保持稳定而易扫描。

### Frontend Page Standard

1. 页面以明亮、低噪声的毛玻璃表面为基础：使用半透明白色表面、细白色边框、克制阴影和 `backdrop-filter`，但不把每一个内容块都包成卡片。
2. 所有页面复用 `--page-bg`、`--surface-glass`、`--surface-glass-strong`、`--surface-border`、`--surface-shadow`、`--glass-blur`、`--ink`、`--muted-ink` 和 `--accent`。新页面不得自定义另一套近似颜色、阴影或毛玻璃参数。
3. 桌面使用居中的最大宽度工作面；手机（不超过 640px）切换为全宽、`100dvh` 工作面。移动端页面内容独立滚动，底部导航或输入栏固定在可触达区域，并为安全区域预留空间。
4. 手机端设置和数据表单必须单列显示，长文本、JSON、绑定信息和按钮在窄屏下可换行且不横向溢出；触控操作的最小高度为 40px，主导航操作为 44px。
5. 聊天是例外工作面：会话顶栏保持透明玻璃，消息窗口为唯一滚动区域，输入栏常驻底部。列表页按最近会话排序，详情和治理配置采用渐进披露，不侵占聊天列表。
