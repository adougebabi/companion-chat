# 2026-09-14

## 任务 09-14-persona-takeover-working-prompt：完成 R01 代码盘点与规划设计

**背景**：Trellis 任务「多重人格接管、Working Persona 与 Prompt 收敛」，此前只完成 `prd.md` + `request.md`（28KB 原始需求，1–20 节）。本次继续 Phase 1 规划。

**产出**（均在 `.trellis/tasks/09-14-persona-takeover-working-prompt/`）：

- `research/inventory-turn-chain.md` — 21 步端到端调用链表（带 `文件:行号`）、候选冻结生命周期、副作用落点清单、`conversation.reply` 与 QUERY continuation 真实实现、事务/恢复/执行资格校验
- `research/inventory-prompt-schema.md` — Prompt 组装链、消息布局与裁剪顺序、Main response schema 字段→消费者表、预算设施、重复序列化审计
- `research/inventory-persona.md` — 人格数据模型、切换入口、`forced_activation` 全链路、Evolution Overlay、三处人格序列化出口、第 16 节线索逐条核实
- `research/inventory-tests-specs.md` — Fake/Spy/Clock 基础设施现状、静态守卫清单、相关规范硬约束、构建命令、环境阻塞
- `design.md` — 技术设计（职责迁移表、forced_activation 逐条迁移表、契约设计、守卫新契约、风险与 3 个待评审问题）
- `implement.md` — 12 阶段执行计划 + G0/G1/G2 检查点 + R1–R5 回滚点
- `implement.jsonl` / `check.jsonl` — 已填充真实 spec 与 research 条目
- `prd.md` — 「当前证据状态」更新为盘点完成 + 关键结论

**关键技术结论**（后续实施必读）：

1. **仲裁插入点**：`mutations.go:888`（`PersistTurnDecision`）之后、`:917/:925`（读回候选 / `prepareCapabilityInvocations`）之前。此窗口内 A 候选已持久但零副作用。
2. **结算事务以 `cognition_frozen_actions.payload` 为唯一事实源**（`mutations.go:1122`）。因此「拒绝 A 的全部副作用」= 覆盖 payload，无需在 `applyPersonalityDecisionPlanTx` / `applyFrozenCognitiveStagesTx` / `conversation_messages` / `settleDeferredCapabilitiesTx` / `completeTurnCognitionTx` 五个点分别打补丁。
3. `conversation.reply` 无二次模型改写（`builtin_capabilities.go:120/123`），私聊发送路径唯一，无 `visible_reply` 旁路。
4. `cognition_frozen_actions` 只有 `frozen/completed/failed`，**无 rejected/superseded 状态** → 被拒候选记录在同一 payload 的 `takeover.rejected_candidate`。
5. Judge 用独立 Provider 角色 `takeover_judge`：`providerBindingRole`（`diagnostics.go:166`）会映射到 `generic_llm`，`model_run`/`provider_provenance` 按 role 记账 → 独立计费与延迟，**无需新表**。需加入 `validProviderRole`（`provider.go:127`）白名单。
6. `forced_activation` 是 `openObjectSchema()` 死字段（`provider_schemas.go:497`），**仓库内 0 条具体规则数据**（默认 `{}`）→ R07 交付归一化契约 + 三分类器 + 稳定 ID + 诊断，不虚构迁移条目。
7. 不存在服务端时间规则自动切换引擎；WakeUp/Reflection 只消费 `active_profile_id`，不切换。
8. 测试基础设施缺口：**无 Fake Judge、无可控时钟、无统一 `newTestApp`**；fake provider 只存在于 HTTP transport 层（`projectHealthRoundTripFunc`）。
9. 静态守卫 `capability_core_test.go:1026` 硬断言 `mutations.go` 中 `StructuredAssembledWithToolsSchema(` 恰好 1 次 → 设计上把 B 的主生成放进新文件 `turn_takeover.go`，使该计数保持 1。
10. 第 16 节「时区与 TOON 递归」复核**不成立**，不改动。

**状态**：Phase 1 规划产物齐备，等待 G0 评审（3 个待定问题见 `design.md` §9 / `prd.md` 末节），评审通过后跑 `task.py start` 进入 Phase 2。

## 任务 09-14-persona-takeover-working-prompt：外部审核（F01–F14）逐条代码核实

**背景**：收到外部审核 `yaoguang_persona_design_review.md`（14 条问题，F01–F14）。逐条回到源码核实，产出 `review-response.md`（同任务目录）。

**核实结论**：10 条成立、4 条部分成立。设计需实际修改的关键项：

1. **F02（最重要）**：可见文本权威来源写错了。真实取值在 `mutations.go:830` + `:1107`：`firstString(responsePlan["visible_text"], decision["visible_text"])`，**仅当为空时**才回退到 `conversation.reply` 参数文本（回退结果会回写进这两个字段），最终 `mutations.go:1149` 用该值写 `conversation_messages`。`ExecuteDeferredTx`（`builtin_capabilities.go:123`）只校验参数、不参与选择。→ CandidatePreview 必须用同一权威表达式，或在冻结时归一化并拒绝冲突来源。
2. **F05**：`resolveCapabilityAction`（`capability_runtime.go:601-618`）**不做校验**（未知能力名直接 continue → 返回 no_op）；工具存在性 + `invocation.Validate` 在 `prepareCapabilityInvocations`（`:181-195`，由 `mutations.go:925` 调用）**即仲裁点之后**。仓库已有便宜的确定性校验器 `validateCapabilityInvocationsForPersistence`（`:205-220`），唯一调用方是 `wakeup.go:530` → 仲裁点前直接复用它即可。
3. **F01**：B 的 decision 若含合法 `personality_decision`，结算会无条件 `applyPersonalityDecisionPlanTx`（`mutations.go:1126` / `:956` / `:1303` → `personality_runtime.go:227-244` 改写 `active_profile_id`）→ takeover_once 语义被破。守卫必须拦在结算权限处，不能只数 `turn_takeover.go` 里的直接调用。
4. **F03**：`RowsAffected` 不是 CAS（`cognition.go:474/488/502` 条件只有 `id=$1 AND status='frozen'`）；payload 覆盖字段不完整（顶层还有 `capability_context_snapshot` / `context_reference_*` / `influences` / `goal_refs` / `intention_refs`）；**per-invocation `ContextSnapshot` 在 `PersistTurnDecision` 内计算（`cognition.go:429-439`），B 不走该函数会缺 snapshot**。既有防线：10 分钟 inbox claim 租约（`cognition.go:121-123`）+ `earlierPending`（`:129-135`）+ `requireCognitionAuthorityRevisionsTx`（`mutations.go:1123`）。
5. **F04**：多处读写按 `active_profile_id` 作用域——`relationship_capability.go:68-73`、`relationship_edit.go:20-23`、`context_reference.go:167/245/252/258`、`evolution_overlay.go:419-421`。B 接管必须重建 profile-scoped 读上下文与写入主体；`resolveTurnPersonaScope` 必须在 A 生成**前**固定快照。
6. **F06**：`normalizeConversationResponseMode`（`query_continuation.go:38-47`）只在 pure query 时返回 `query_continuation`，混合批次返回 `final` → `design.md:313` 的跳过条件不会跳过混合轮，与 `:308` 矛盾。
7. **F08（纠正需求假设）**：`applyPersonalityDecisionPlanTx` 调用方只有 `mutations.go:956/1126/1303`（turn 结算）；`applyPersonalityDecision`（`personality_runtime.go:260`）是**死代码**。WakeUp/Reflection/API **都不切换**。今天唯一的切换机制 = Main 读到规则后输出 `personality_decision`，且 `switching.rules[].condition` 确实进 System Persona（`provider_prompt_composer.go:261-285` 显式保留，`:318` 保留 condition）。`request.md:192`「持续切换由 WakeUp/Reflection 处理」与代码不符。
8. **F07**：`testdata/initialization/dense_multi_card.txt` 就是自然语言多重人格卡，明写「遇到强烈的公开质疑时星火可强制接管」→ 真实旧卡有可迁移语义；我的「缺 `mode` → unclassified」会让旧卡永不接管。审核引用的 `conditions/target_profile/trigger_profile` 示例不在本仓库，无法核实。
9. **F09/F10/F11**：`stringSchema()` 无 `maxLength`（`provider_schemas.go:31`）；静态守卫是源码文本计数（`capability_core_test.go:1032`）不能代替运行时预算；`normalizeEvolutionPersonalityBaseline`（`evolution_overlay.go:452-458`）**静默把非对象 `traits` 替换成 `{}`**，数值路径只有大五类（`:36-40`），散文特质会全丢。
10. **F12**：`settleDeferredCapabilitiesTx` 在结算事务内（`mutations.go:1157` in `:1122`），`capability_runtime.go:363-365` 注释与实际不符。但内置 deferred 能力只写本行/落 intent（`builtin_capabilities.go:123-136`、`:243-295`），**无网络 IO**，严重性低于审核表述。
11. **F13**：`implement.jsonl` / `check.jsonl` 第 1 行的 `_example` 占位行**仍在**，且 research 文件被登记为权威上下文 → 需清理并标注 research 旧建议 superseded。
12. **F14.4 是我的盘点错误**：`recentPromptFragments`（`provider_context.go:133-135`、`:164`）**保留** `role=user/assistant`，盘点里写的「recent 全写成 user」是错的，应改盘点而非改代码。

**不同意审核之处**（写在 `review-response.md`）：① 审核隐含「持久切换应彻底离开 Main」，但代码里 Main 的 `personality_decision` 是今天唯一能工作的切换机制，搬空会打断白天/晚上切换 → 正确做法是逐场景命名显式入口 + 禁止 takeover 写 active；② F12 实际无网络 IO 在事务内；③ F09 说我「称已实现不思考」，但 `design.md:193` 原文已如实写明未开启。

**状态**：等待用户对 G0 三问 + 上述收敛方案拍板，然后修订 `design.md` / `implement.md` / research / jsonl。

## 任务 09-14-persona-takeover-working-prompt：R1 设计收口（用户决议 + 新证据）

**背景**：用户接受事实纠正，但明确**不按上一版审核的过度归因扩大重构范围**（`craft` 模式，Agent 模式编辑）。本轮完成设计与计划的 R1 修订。

**用户决议（已写入 `design.md` §11）**：
1. **保留 Main 驱动的持久切换**，收敛为「逐调用场景明确授权」；**不实现**确定性时间引擎（选项 A）；**不新增** WakeUp/Reflection 切换。
2. 持久切换与接管分开：有权的原始 Main 可提 persistent switch；Judge 只判 takeover；**接管后的 B 无权改 persistent active**。
3. 权限必须在**候选校验 + 结算 + 恢复**三处执行，不能只删 Prompt 字段。
4. Main 保留持久切换规则，但**不注入** takeover rules，**不恢复**完整非激活人格。
5. F12 降为文档/注释修正（**不改**结算事务）。
6. F09 撤回「声称已关闭 thinking」的归因；复用当前模型绑定可接受，参数/限制/成本如实报告。
7. 其他审核项按证据核对，**不因没反驳就视为已确认**。

**新增关键事实（本轮实测，已改写原结论）**：

| 编号 | 事实 | 位置 |
|---|---|---|
| C1 | **`forced_activation` 不是 0 条规则**（推翻旧结论）：仓库自带多头卡散文声明「遇到强烈的公开质疑时星火可强制接管」，且 `dense_multi_expectations.json` 断言 `id=forced` 要求产出 `forced_activation.contains="公开质疑"`（basis explicit）。唯一校验路径是 live provider（`provider_live_tool_test.go:21-23` → `liveProviderConfig` `:443-447` 需 `FLUCTLIGHT_LIVE_PROVIDER_TEST=1`）→ **本地未验证** | `testdata/initialization/dense_multi_card.txt`（300 字纯散文）；`dense_multi_expectations.json` |
| C2 | **时区缺陷成立**（推翻我自己的否证）：`compactMessageTime` 对历史时间戳 `parsed.UTC().Format("01-02 15:04:05")` → 强制 UTC 且**无时区标识** | `provider_context.go:626-629` |
| C3 | **A 的审计行零读者**：`cognition_assessments` / `cognition_decision_proposals` 全仓只有 INSERT，无任何非测试读者 → F03 最后一条由实证关闭 | `cognition.go:455/458`；`migrations/runner.go:187/188` |
| C4 | **请求大小设施已存在**（更正我的概括）：token 估算 + 三层 cap + `PromptAssemblyTrace`（含 `EstimatedInputTokens`/`SectionTokens`）+ 确定性守卫；缺的是分节字节数、Judge 覆盖、机器可读报告 | `prompt_context_assembler.go:14-19/38-48/215-225/255-259/397-399`；`provider_live_tool_test.go:34-47`；`prompt_context_assembler_test.go:186-187` |
| C5 | 静态守卫确为**源码文本计数**（`strings.Count`），不能替代运行时预算 | `capability_core_test.go:1026-1035` |
| C6 | recent 消息**保留真实 role**（user/assistant）：`assemblePromptMessages` 原样 append recent fragment → 改盘点不改代码 | `provider_context.go:133-135/164`；`prompt_context_assembler.go:370-390` |
| C7 | `filterSemanticProfileValue` 只对 `id/name/rules/edges` 特判，其余键回落 `filterCorePersonaValue` → **非激活 profile 的内容字段确实进 Main prompt**；`compactPersonalitySystem` 用 `cloneMap(system)` + 追加 `active_profile` 整对象 → 人格三处重复 | `provider_prompt_composer.go:261-285/299-322`；`provider_context.go:289-315` |
| C8 | 持久切换唯一执行链：`preparePersonalityDecision`（含 trigger 白名单校验、cooldown、target 存在性）→ `applyPersonalityDecisionPlanTx`（revision CAS） | `personality_runtime.go:83/158-184/227-257` |

**本轮改动文件**（均在 `.trellis/tasks/09-14-persona-takeover-working-prompt/`）：
- `design.md` — **重写**（714 行）：新增 §0 修订记录 + **权限模型** + §0.3 逐调用场景授权表 + **§0.4 权限执行脊 E1–E5**；§3 重写（R07 事实更正 + 选项 A 已决议）；新增 §4.3 唯一可见文本权威、§4.9 无副作用候选校验、§4.10 F12 文档级修正；§4.8 新增 `turn_stage` 阶段机与恢复全枚举；§8 补运行时预算；§10 A/B 作用域读写表；§11 已决议事项
- `implement.md` — **重写**（474 行）：新增**阶段 2.5 权限执行脊**（先于仲裁接入）；阶段 9「确定性求值」**取消**；阶段 2 补真实卡回归 fixture；阶段 4 加 `resolveCanonicalVisibleReply` + `TestJudgeSeesCanonicalTextThatIsActuallySent`；阶段 8 时区改为成立；验证命令改用 `go -C`（F14.5）
- `prd.md` — 关键结论按 R1 更正；评审确认项标记已关闭
- `request.md` — **5 处**「已有切换入口」错误描述更正（§1 表下、§11 首段、§15 并发段、§17.1 场景表、§19 清理段），均带 `[更正 2026-09-14，基于代码证据]`
- `research/*.md` — 四份文件加层级声明 banner；turn-chain 4 处 + persona 2 处标 `superseded`；persona/prompt-schema/tests-specs 补 R1 更正
- `implement.jsonl` / `check.jsonl` — **清除 `_example` 占位行**（F13）
- `review-response.md` — 新增「用户决议与采纳结果」+「补充核实 C1–C6」+「仍未解决/未验证 U1–U6」

**文档层级约定（本项目本任务）**：`request.md` > 经评审的 `design.md` > `implement.md` > research 旧建议（非权威）。原始需求若与代码不符，**就地更正并留带日期的证据标记**，不静默改写。

**状态**：R1 收口完成，待跑 `task.py start` 进入实施（阶段 1 测试基座）。

## 任务 09-14-persona-takeover-working-prompt：实施阶段 2（规则归一化）与阶段 2.5（权限执行脊 E1–E5）

**背景**：用户「可以，开始处理吧」→ 跑 `task.py start`（status → in_progress），进入实施。

**本地测试环境（关键，后续复用）**：
- OrbStack：`~/.orbstack/bin/orbctl start`（本机无 Docker Desktop）
- 容器：`docker run -d --name lac-test-pg -e POSTGRES_PASSWORD=fluctlight -e POSTGRES_USER=fluctlight -e POSTGRES_DB=fluctlight -p 55432:5432 pgvector/pgvector:pg16`
- 迁移：`CORE_GO_DATABASE_URL='postgres://fluctlight:fluctlight@127.0.0.1:55432/lac_test?sslmode=disable' go -C apps/core-go run ./cmd/migrate`（不跑迁移 → 测试全红 `relation does not exist`）
- 测试：`GO_CORE_TEST_DATABASE_URL='postgres://fluctlight:fluctlight@127.0.0.1:55432/lac_test?sslmode=disable' go -C apps/core-go test -count=1 ./internal/core/`
- 基线全绿 `ok ~44s`；当前全绿 `ok ~53s`

**阶段 2 产出**（新文件 `apps/core-go/internal/core/persona_switch_rules.go` + `persona_switch_rules_test.go`，全绿）：
- `normalizePersonaSwitchRules(corePersona, runtime, subjectProfileID)` 统一归一化 `switching.rules[]` / `switching`（list）/ `switching.default_profile_id` / `takeover_rules[]`（新 typed）/ `forced_activation`（legacy 开放对象）。
- 规则种类：`persistent_deterministic` / `persistent_semantic` / `turn_takeover` / `unclassified`；**只有 `turn_takeover` 可执行**。
- 稳定 ID：`switch:<id>` / `takeover:<id>` / `takeover:forced:<digest>` / `switch:forced:<digest>` / `switch:index:<n>`。`RuleContentDigest` 覆盖内容指纹（改 condition 文本变 digest 不变 ID）。
- `Enabled` 语义 = 「声明已解析完整（target 存在、无歧义）」，**不是**「可执行」。
- 诊断码有界：`unclassified` / `target_profile_unresolved` / `target_profile_ambiguous` / `switch_rule_id_missing` / `source_profile_unresolved` / `profile_index_empty`。
- 真实卡回归：`TestPersonaSwitchRulesNormalizeTheRealMultiProfileCard` 绑定 `testdata/initialization/dense_multi_card.txt`，抽出真实双分句「遇到强烈的公开质疑时星火可强制接管；收到 actor_user 明确的安全确认后暮光可重新主导。」→ 1 条可执行 `takeover:forced:`（星火）+ 1 条 persistent_semantic（暮光）。（F07 要求：不能只用理想 typed fixture 证明成功。）
- `resolvePersistentSwitchGrant(scope, scenario, rules)`：`takeover_reply` / `query_continuation` / `takeover_judge` **无条件拒绝**；`cognitive_assessment`（Main）需声明了 switching 规则才授权。

**阶段 2.5 产出**（新文件 `persistent_switch_gate.go` + `persistent_switch_gate_test.go`，全绿 13 例）：
- **E1** `applyPersistentSwitchGrant(decision, grant)`：未授权 → 删除 `personality_decision` + `personality_transition`，记 `persistent_switch_proposal`（只留原始标识，不泄漏 plan），保留用户可见回复。
- **E2** `PersistTurnDecision(..., authority turnDecisionAuthority)` 新签名（第 7 参）；payload 写 `persistent_switch` 标记 + `turn_persona_scope`；`validatePersistentSwitchPersistence` 兜底：有 transition 无正授权 → `personality_switch_not_authorized`。
- **E3** `applyPersistentSwitchIfAuthorizedTx(ctx, tx, id, payload, plan)` = **唯一**可写 `active_profile_id` 的入口；`plan==nil` 或 `payload.persistent_switch.authorized!=true` → 结构性 no-op（replay 幂等，不报错）。**mutations.go 三处结算点（主回复 ~970 / QUERY 续写 ~1140 / 恢复 ~1317）全部改走它**。
- **E4** 恢复路径复用同一 gate，从 frozen payload 读授权，**不重新推导**。
- **E5** `resetPersistentSwitchAuthorization(payload, scenario, reason)`：覆盖候选时必须调用，授权**不继承**。
- 删除死代码 `personality_runtime.go` 的 `applyPersonalityDecision`（未引用，但是绕过 E3 的写路径）。
- 静态守卫 `TestPersistentSwitchStaticGuardOnlyGateWritesActiveProfile`：断言 `mutations.go` 直调 `applyPersonalityDecisionPlanTx` = 0、调 gate = 3、E1 调用 = 1；gate 文件直调 = 1；`applyPersonalityDecision` 不得重现。
- 真实 DB 守卫测试：`TestTakeoverReplyCannotWritePersistentActive`（接管方无法改 active）、`TestPersistentSwitchStillWorksViaAuthorizedScenarios`（Main 授权路径仍能改 active）、`TestUnauthorizedScenarioHasNoPersonalityDecisionField`、`TestRecoveryDoesNotApplyUnauthorizedSwitch`。

**踩坑记录**：
1. **E1 曾被漏插**：摘要声称已插入，实际 `mutations.go` 无 `applyPersistentSwitchGrant` 调用 → 必须先 grep 验证，不能信摘要。已插在 `preparePersonalityDecision`（~822 行）之前。
2. `replace_all` 未覆盖全部同形调用点：三处结算点原文相同，但一次 `replace_all` 只改了 1 处 → 逐处核对行号（970/1140/1317）。
3. `PersistTurnDecision` 改签名后，`cognition_growth.go:366` 与 `project_health_transaction_integration_test.go:1064` 两处调用点需补 `turnDecisionAuthority{}`（非 turn 上下文，零值=无授权），否则编译失败。

**状态**：阶段 2 + 2.5 完成并全绿（`ok 53.3s`）。下一步**阶段 3：Working Persona 投影与 Prompt 收敛**（新增 `working_persona.go`；改 `provider_context.go` / `provider_prompt_composer.go` / `provider_schemas.go`）。阶段 1 测试基座（Fake Provider/Judge）推迟到阶段 4 需要时再做——阶段 2/2.5 的测试不需要它。

## 任务 09-14-persona-takeover-working-prompt：实施阶段 3（Working Persona 投影与 Prompt 收敛）

**产出**（全量测试全绿 `ok 80.4s`，vet 干净）：

新增 `apps/core-go/internal/core/working_persona.go` + `working_persona_test.go`：
- `projectWorkingPersona(projection, subjectProfileID) (map[string]any, workingPersonaTrace)`：确定性、不调模型。三层合并 = Shared Identity（`identity` + `life_profile`）→ profile 基线（`personality_system.profiles[subject]` 优先，回落顶层 `personality`/`behavioral_policy`）→ 活跃 overlay（`projection.EffectivePersona`，仅当 subject == runtime active）。
- envelope：`shared_identity` / `working_persona`（含 `profile_id`）/ `profile_id` / `persona_revision` / `overlay_revision`。`workingPersonaTrace{Included,Excluded,Verbatim,OverlayApplied,...}` 让「散文特质被静默丢弃」这类回归可观测。
- 排除：其他 profile 的内容、`visual_identity`（走独立投影）。**未排除 `secrets`**（见下「设计冲突」）。
- `systemPersonaForProjection(projection, schemaName)`：组装 System Persona bundle；**只有 `schemaName == conversation_turn_response`（交互式 Main 轮）才注入 `persistent_switch`**——WakeUp / daily_review / native_cognition 共用 `assembleProjectionPrompt` 且 role 也是 `cognitive_assessment`，所以必须按 schema 而非 role 把关（用户明确禁止新增 WakeUp/Reflection 切换）。

`provider_prompt_composer.go`：
- `filterCorePersona` 重写为唯一 System Persona 出口：渲染 `shared_identity` + `working_persona` + 条件 `persistent_switch` + `extensions` + 精简 `personality_system`；**永不渲染 `takeover_rules`**。
- `filterPersonalitySystem`：`takeover_rules` 显式丢弃；`profiles` 走新的 `filterProfileRosterValue/List`，**只保留 `{id,name}`**（修掉 C7/F01/R09 的「非激活人格内容漏进 Main prompt」）。
- `deriveWorkingPersonaBody`（legacy/初始化抽取路径）也读取 `personality_system.profiles[active]`，否则单 profile 卡会丢掉全部决策输入。

`provider_context.go`：
- `assembleProjectionPrompt` 改传 `systemPersonaForProjection(projection, schemaName)`。
- `workingMemoryInputFromProjection` 去重 delete 列表扩展为 `core_persona`/`memories`/`recent_messages`/`effective_persona`/`personality_system`。
- `compactPersonalitySystem` 精简为仅 `active_profile_id`/`previous_profile_id`/`runtime_revision`（不再 `cloneMap` 整组 profiles、不再追加 `active_profile` 整对象）。

`evolution_overlay.go`：`normalizeEvolutionPersonalityBaseline` 遇非对象 `traits`/`expression` **不再静默替换为 `{}`**；散文逐字保留到 `{description, source_shape}` 载体（F11）。新增 `evolutionDescriptiveValue` / `evolutionValueShape`。

新增测试 12 例（全部 PASS，非跳过）：WorkingPersona 保真散文、排除他人格/视觉资产、确定性、overlay 只对活跃人格生效、非活跃人格不套 overlay；`TestProviderSystemPersonaHasNoTakeoverRules`、`TestProviderSystemPromptHasSinglePersonaSource`、`TestProviderRuntimeContextCarriesNoSecondPersonaCopy`；Evolution 散文/结构化 traits。

**关键设计冲突（未擅自按设计文稿执行，留待产品确认）**：
1. **`secrets` 不能排除**：设计稿写「排除 `secrets` 全文」，但既有硬契约 `TestComposeProviderMessagesPreservesMultiPersonalityDecisionInputs`（`provider_prompt_composer_test.go:193`）要求 Main 的人格决策输入包含活跃 profile 的 `secrets.information_asymmetry`（断言 `隐藏`）以及 `voice.speed`/`body_language`/`behavior_loops`/`intimacy_progression`/`desires`。按用户「不为关闭审核项改行为」的指示：**保留现状**，在 `working_persona.go` 顶部以注释记为 open item。
2. **活跃 profile 的内容必须留在 Main System Persona**：不能只渲染 `{id,name}` 后把内容挪走；`working_persona` 必须携带活跃 profile 的全部声明键。
3. **`forced_activation` 未从 Main 摘除**：`persistentSwitchPromptSection` 只surface `Source` 前缀为 `switching` 的规则，而 forced_activation 派生的 `persistent_semantic` 规则（如 dense_multi_card 的「暮光可重新主导」）Source 是 `forced_activation`，不会被 surface。若此刻从 Main 摘掉 `forced_activation`，会同时丢掉「Main 保留持久切换规则」的能力（回退风险）。故本轮只摘 `takeover_rules`，把 forced_activation→persistent_switch 的归一化衔接留给阶段 5/6（仲裁点）。

**踩坑**：
- `containsString` 已存在于 `goal_intention.go:804`，测试里不要再声明（编译冲突）。
- `replace_all`/摘要都不可信，改动后必须 grep 复核真实调用点。
- 测试命令与迁移命令见本文件上一条记录。

**状态**：阶段 2 + 2.5 + 3 完成。下一步**阶段 4：唯一可见文本、候选预览与 Judge 契约**（新增 `visible_output.go` / `turn_takeover.go`，改 `provider.go` / `diagnostics.go` / `provider_schemas.go`）——此阶段需要先补阶段 1 的测试基座（Fake Provider/Judge、`newTestApp`、`captureProviderWirePayload`）。

## 任务 09-14-persona-takeover-working-prompt：实施阶段 1（测试基座）与阶段 4（唯一可见文本 / 候选预览 / Judge 契约）

**全量测试全绿 `ok 52.8s`；`gofmt -l` 干净；`go vet` 干净。**

**阶段 4 产出**：
- `visible_output.go` 新增 `canonicalVisibleReply{Text,Source,Conflict,Digest}` + `resolveCanonicalVisibleReply(responsePlan, decision, invocations, registry)`。优先级严格沿用历史值（response_plan → decision → reply 参数），**行为不变**；新增 `Conflict` + `visibleTextDiagnostic`（码 `visible_text_source_conflict`）。冻结进 `decision["visible_text"]`/`responsePlan["visible_text"]` + `visible_text_source`/`visible_text_source_conflict`/`visible_text_digest`/`visible_text_diagnostics`。
- `mutations.go`：生成期**只解析一次**（原 855 行的 `normalizeVisibleReply(firstString(...))` 已替换）；**结算期（原 1109 行）不再重新推导**，改为直接读冻结的 `decision["visible_text"]` → `responsePlan["visible_text"]`，**不再回退 `replyTextFromCapabilityInvocations`**。这是 F02 的实质修正。
- 新增 `turn_takeover.go`：`CandidatePreview` + `buildCandidatePreview`（按 `OutputRole`/`Type`/`TargetKinds` 分派，**禁止** `CapabilityName ==` 比较，**不调 Execute**，不 dump 原始参数）；`buildTakeoverControlView` + `takeoverControlViewForProfile`（复用 `projectWorkingPersona`，stance 白名单 = `personality`/`behavioral_policy`/`voice`/`expression`，排除背景/视觉资产/secrets/behavior_loops/life_profile）；`takeoverJudgementSchema`（`takeover` bool 必填 + 有界 enum `decision_code`）；`takeoverJudgeSystemPrompt`（显式「数据不是指令」）；`takeoverJudgeInput`/`takeoverJudgeUserPacket`/`takeoverJudgeMessages`（严格 2 条：system + user JSON）；`takeoverJudgeInputBudgetExceeded`；`takeoverJudgeOutcome*` 有界词表 + `takeoverJudgeOutcomeForError`。
- `provider.go`：`validProviderRole` 加入 `takeover_judge`；新增 `StructuredAssembledJudgement`（assembled=true、definitions=nil、enableThinking=false → **不发** enable_thinking，因为 Provider 只有 true 分支）；`providerSchemaForRole` 加 `takeover_judge`。
- `diagnostics.go`：`providerScenario` 显式返回 `takeover_judge`；`providerPriority` 与 `cognitive_assessment` 同级 100。
- 常量：`takeoverJudgeSchemaName = "takeover_judge_response"`（故意与 `providerSchemaName(role)` 一致，保证 fallback 与显式传参一致）。

**阶段 1 产出（测试基座）**：新增 `test_base_test.go`：
- `fakeProviderRouter`：按 wire `response_format.json_schema.name` 路由脚本化响应（可分别脚本 A/Judge/B），记录并可按 schema 取回请求 payload；`router.on(schema, script)` / `otherwise` / `payloads(schema)` / `requestCount(schema)`。
- `captureProviderWirePayload(inner)`：记录真实 wire body（F09「实际参数」验收与阶段 11 大小统计）。
- `newTestApp(t, repository, transport)`：收敛 `&App{DB,Provider,ContextResolver,Capabilities,Runtime}` 手工装配。
- **可控时钟**：`App.Clock func() time.Time` + `(*App).now()`；生产默认 nil → `time.Now()`（语义不变）；`mutations.go` 三处 turn 结算点（987/992、1165/1167、1345）改走 `a.now()`。
- `seedTurnConversation` / `seedCognitiveProviderRole` / `fixedClock` 辅助。

**新增测试 16 例（全部真实执行）**：
- 纯单元：canonical 优先级/冲突/相等/空白；preview 无副作用 + 按元数据分派（反向用例：名为 `conversation.reply` 但 `OutputRole=moment`）；control view 有界（断言不含背景/视觉/secrets/life_profile/behavior_loops，含 stance）；Judge 消息恰好 2 条且满足 `validAssembledProviderMessages`；Judge schema 有界（required 只有 takeover、decision_code 是 enum、不暴露 target/mode/text）；预算守卫；outcome 词表；role→generic_llm 映射 + scenario + priority。
- 真实 DB + wire：`TestJudgeWirePayloadHasNoUnsupportedFields`（无 tools/tool_choice/enable_thinking，有 json_schema response_format，恰好 2 条 messages）；`TestTakeoverJudgeHasIndependentModelRun`（`diagnostic_model_runs` 里 `role='takeover_judge'` 且 `binding_role='generic_llm'`）。
- **F02 端到端**：`TestSettlementWritesCanonicalVisibleTextAndRecordsConflict`（root/response_plan/reply 三源冲突 → `conversation_messages.text` == response_plan 文本，冻结 decision 记录 conflict/source/diagnostic）。
- 静态守卫：`TestVisibleTextStaticGuardHasSingleAuthority`（`mutations.go` 只有一个解析点、不再出现 `replyTextFromCapabilityInvocations(`、preview 无 `CapabilityName ==`、无 `.Execute(`、读 `canonical.Text`）。

**踩坑**：
1. `recordQueuedModelRun` 落表是 **`diagnostic_model_runs`**（不是 `model_runs`），列含 `role` 与 `binding_role`。
2. `http.ErrHandlerTimeout` 的文本含 "timeout" → 会被 `isProviderTimeout` 判为超时；测通用失败要用别的错误。
3. `previewRegistry` 直接构造 `&CapabilityRegistry{capabilities, definitions}`，绕开 `NewCapabilityRegistry` 的 `Register` 校验（测试想声明「名字像 reply 但角色不是」的畸形定义）。
4. 断言 map 里的 `any` 不能直接 `string(...)`，要 `stringValue(...)`。

**未做/遗留（有意，记为 open item）**：
- **`cognitiveTurnResponseSchema` 未条件化**（设计 §4.6 要求 `grant.Allowed==false` 不含 `personality_decision`）。原因：`grant.Allowed` 只认 `Source` 前缀为 `switching` 的规则，而 `forced_activation` 派生的 persistent 规则 Source 是 `forced_activation`；若此刻条件化，只声明 forced_activation 的实例会**同时**丢掉 prompt 段与 schema 字段（双重回退）。按用户「不为关闭审核项改行为」的指示，留到阶段 5 与 forced_activation 归一化衔接一起做。`internal_intent`（可选、maxLength 120）同理留到阶段 5。
- Judge 尚未接入 turn 管线（阶段 5 的仲裁点）；`takeoverJudge*` 目前只有契约与 provider 能力，无生产调用点（这是设计上的分阶段）。

**下一步**：**阶段 5：仲裁点接入、阶段机与 B 生成**（任务 #29）。

## 任务 09-14-persona-takeover-working-prompt：实施阶段 5（仲裁点接入、阶段机、B 生成）与链路级验证

**产出**（新增 `turn_takeover_chain_test.go` 9 例；全量 core 测试全绿 `ok ~70s`；`gofmt -l` / `go vet` 干净）：

- 阶段 5 实现全部完成（上一轮已完成主体编码）：`turn_decision.go`（A/B 共用 `normalizeTurnDecision`）、`takeover_scope.go`（`resumeProjectionForReplyOwner`）、`turn_takeover.go`（`applyTurnTakeover` 单仲裁点 + 阶段机 + `generateTakeoverReply`）、`cognition.go`（`AdvanceTurnStage` / `ReplaceFrozenTurnDecision` / `LoadFrozenTurn` 阶段校验）、`mutations.go`（唯一插入点 + 执行资格门）。
- 任务板 #29 / #30 / #9 / #11 / #31 / #12 全部关闭。

**链路级测试首跑暴露并修复 3 个只有端到端才会出现的真实缺陷（单元测试与静态守卫都照不到）**：

| # | 缺陷 | 症状 | 修复 |
|---|---|---|---|
| **D1** | **Core Persona 信封未拆包**：`buildPersonaProfileIndex` / `normalizePersonaSwitchRules` / `initialPersonalityProfileID` / `projectWorkingPersona` / `systemPersonaForProjection` 直接读 `projection.CorePersona["personality_system"]`，但投影存的是 `{"authority":"hard_constraint","data":<core_persona>}` | 生产环境 `switching.rules` / `takeover_rules` **完全不可见** → Main 永远拿不到持久切换授权 → 阶段 5d 的条件化 schema 把 `personality_decision` 从 Main response schema **整个移除**（回归既有能力）；接管规则永远选不中。阶段 2/3 单测全绿是因为它们直接传**未拆包**的 map | 新增单一访问器 `corePersonaData()`（`persona_switch_rules.go`；有 `authority` 标记才拆 `data`，无标记原样返回、幂等），5 处 persona 语义读取点统一使用 |
| **D2** | **覆盖候选未剥离 provider codec 旁挂字段**：`PersistTurnDecision` 会删 `decision["tool_calls"]`/`["capability_invocations"]`，`ReplaceFrozenTurnDecision` 没删 | 接管后 payload 同时有 `decision.tool_calls` 与根级 `capability_invocations` → 双权威 → `handled` 分支 `LoadFrozenTurn` 报 `capability_runtime_dual_authority` → **每一次接管都失败** | 抽出 `stripFrozenDecisionSidecars()`（`cognition.go`），两处写入共用 |
| **D3** | **接管生成复用 Main schema 名**（`conversation_turn_response`） | `systemPersonaForProjection` 按 schema 名注入 `persistent_switch` 段 → 接管 prompt 拿到「持久主导人格切换」规则段，而它自己的 schema 已禁掉 `personality_decision`（自相矛盾 + 泄漏 switching 规则给接管方） | 引入 `takeoverReplySchemaName = "takeover_reply_response"`；接管生成用它，并用静态守卫钉住「Main schema 名不得出现在接管生成里」 |

**链路级测试方法（后续阶段复用）**：`isolatedCoreTestRepository`（每次新建隔离库）+ `takeoverChainSeed`（多 profile core_persona 落库：`switching.rules` + `takeover_rules` + `fluctlight_personality_runtime`）+ `fakeProviderRouter` 按 wire `response_format.json_schema.name` 脚本化响应。**A 与 B 用不同 schema 名（`conversation_turn_response` / `takeover_reply_response`），Judge 用 `takeover_judge_response`**，因此生成次数可精确断言。断言对象是**持久行**（`conversation_messages` / `cognition_appraisals` / `fluctlight_state_revisions` / `fluctlight_personality_runtime`）+ **真实 wire payload**（system prompt 里的 profile 私有标记），而非单元 helper。

**覆盖**：Judge=false 只执行 A；Judge=true 只执行 B（A 的 appraisal/state 零增量、无 A 文本落库）；takeover_once（active 仍 spark、frozen scope 分离 active/reply_owner）；A/B 作用域（A 的 prompt 含 spark 标记不含 twilight，B 反之，B 含「未发送」边界规则）；B 生成后不再仲裁（judge=1）；纯 QUERY 不调 Judge；非法候选 Judge 前失败；B 生成失败绝不回退发 A；阶段机语义 + 静态守卫（单插入点、无反向仲裁、Judge 单调用、`winner_ready` 资格、E5 重置、M3 `FOR UPDATE`、M4 派生字段重建、信封拆包不回归）。

**未覆盖（顺延阶段 6/7）**：混合 final 轮的仲裁语义与 `takeover_reply_budget_exhausted`；`a_frozen`/`arbitration_decided`/`b_frozen`/`executing` 崩溃注入与并发版本竞争；多 profile 记忆/关系的 A-only·B-only·shared 完整读写矩阵（本阶段只验证了 prompt 侧作用域与 projection 重建）。

**踩坑**：
1. 静态守卫别用「出现次数」猜字符串——`contextProjectionFromValue(decision["context_projection"])` 在 `cognition.go` 有 2 处（`PersistTurnDecision` + `ReplaceFrozenTurnDecision`），计数==1 的假设是错的；改为断言真实语义（`FOR UPDATE` + `stage != overwrite.ExpectedStage`）。
2. `fakeProviderRouter.on(schema, script)` 的 `script` 是 `func(payload) fakeProviderResult`，**不能**在闭包里反引 router 自身（会用到未赋值的变量）；用「按调用序号递进」的状态闭包即可。
3. `HandleTurn` 的错误在「接管后重载」阶段出现：`LoadFrozenTurn` 对 `status='frozen'` 的行会跑 `validateExecutableCapabilityPayload`，所以任何 payload 双权威都会在**这里**炸，而不是在写入时——这正是 D2 只被端到端测到的原因。

**状态**：阶段 5 完成并全绿。下一步 **阶段 6：QUERY 互斥与调用预算守卫（R06/F06）**。

---

## 阶段 6：QUERY 互斥与调用预算守卫（R06 / F06）— 完成

**范围**：互斥语义修正、混合 `final` 轮可仲裁、未返回查询结果的边界、接管回复预算耗尽受控失败、静态守卫与预算链路测试。

**代码改动**（3 个生产文件 + 1 个测试文件）：

- `turn_decision.go`：新增常量 `takeoverReplyBudgetExhaustedCode = "takeover_reply_budget_exhausted"`，`errTakeoverReplyBudgetExhausted` 改为 `errors.New(<该常量>)`——让「记录的错误码」与「错误字符串」不可能漂移。
- `turn_takeover.go`：
  - 新增 `takeoverFailureCode(err) string`：把仲裁失败映射到落库码（预算耗尽取常量，其余 `takeover_failed`）。
  - `takeoverReplyContextRule` 新增一行**系统级**边界规则：「本轮【没有返回任何查询结果】：原候选提出的查询类能力【未被调用】，其返回值不存在；不得把未返回结果的查询当成已知事实」。放在接管上下文而非 `capabilityConversationPolicyInstruction`，因为后者有 900 字符预算门（`provider_prompts_test.go:18`）且被 Main 轮共用。
- `mutations.go`：仲裁错误分支从「`superseded || budgetExhausted` 都直接 return」改为「**只有** `superseded` 不动本行；其余一律 `FailTurnCognition(code=takeoverFailureCode(err))`」。
- `turn_takeover_chain_test.go`：新增 3 个链路用例 + QUERY 互斥静态守卫；`takeoverChainSeed` 参数化为 `takeoverChainSeedWithTakeoverRules(..., rules []any)`，`len(rules)==0` 时省略 `takeover_rules` key（用于「无规则零额外调用」场景）。

**修掉的真实缺陷（D4）**：旧代码对 `errTakeoverReplyBudgetExhausted` **直接 return 且不落失败码**，frozen 行停在 `arbitration_decided`（`judge_a_takeover_b_pending`）且 `status='frozen'` → 可被无限重试；而 B 的结构性禁止（接管发言方不得再花第三次调用）使其**永远无法成功**。这是「受控失败」与「静默可重试死循环」的区别。已加**反向验证**：临时把 `mutations.go` 退回旧分支 → `TestTakeoverReplyQueryContinuationFailsClosed` 精确失败（`got "frozen" / ""`），确认断言有牙。

**关键设计确认（F06 语义）**：互斥的是「**结果依赖型 QUERY continuation 路径**」与「takeover 路径」，不是「候选里出现任意 QUERY 能力」。判据只读**归一化后的 `responseMode`**；静态守卫显式**禁止** `turn_takeover.go` 出现 `CapabilityExecutionPureQuery` / `validatePureQueryContinuation(`，防止语义退化。混合批次（`conversation.reply` + `memory.recall`）→ `validatePureQueryContinuation` 失败 → 派生 `final` → **可仲裁**，链路测试对**冻结后的真实 invocations** 调同一 validator 断言其非 pure query。

**静态守卫新增**：互斥判定的 `strings.Index` **顺序**断言（必须先于 `selectTurnTakeoverRule` / `StructuredAssembledJudgement` / `StructuredAssembledWithToolsSchema`）、`SkipReason: "query_continuation"` 必须落 payload、`takeoverFailureCode(takeoverErr)` 必须存在、错误常量必须被 `errors.New` 复用、`input.ForbidQueryContinuation` 必须在 `turn_decision.go`。

**验证**：全量 `./internal/core/` → `ok 76.949s`；新增 4 例（含 4 子场景表 `TestMainGenerationBudgetNeverExceedsTwo`）全 PASS；`go build` / `go vet` 干净；`gofmt -l` 无输出；`query_continuation_test.go` 全 6 例回归通过。任务板 #32 / #33 / #34 关闭。

**仍未覆盖（顺延阶段 7）**：`a_frozen`/`arbitration_decided`/`b_frozen`/`executing` 各边界的崩溃注入与并发版本竞争；多 profile 记忆/关系 A-only·B-only·shared 完整读写矩阵（目前只验到 prompt 侧作用域与 projection 重建）。

**状态**：阶段 6 完成并全绿。下一步 **阶段 7：恢复、并发与幂等（R10、F03）**。

---

## 阶段 7：恢复、并发与幂等（R10 / F03）— 完成

**范围**：`design.md` §4.8 恢复全枚举、`executing` 阶段真实化、失效快照拒绝、被拒候选隔离与 M10 消费者守卫。

**代码改动（4 个生产文件）**：

- `turn_takeover.go`：
  - `turnStageExecutable` 由「只认 winner_ready」改为 `winner_ready | executing`。
  - 新增 `BeginTurnExecution(ctx, frozenID)` = `AdvanceTurnStage(winner_ready → executing, nil)`。
  - `applyTurnTakeover` 的 `case turnStageExecuting, turnStageSettled: return error` 拆开：`executing → (false, nil)`（不重判、不覆盖、继续执行）；`settled → error`（拒绝重演）。
- `mutations.go`：资格门改为 `!turnStageExecutable(frozen.Payload)`；门通过且阶段为 `winner_ready` 时调 `BeginTurnExecution` 并镜像进 `frozen.Payload`。
- 测试：新增 `turn_takeover_recovery_test.go`（10 例）；`test_base_test.go` 新增 `totalRequests()`（物理 attempt 计数）；`turn_takeover_chain_test.go` 同步 3 处（资格门守卫、settled 阶段断言 → `executing`、`TestTurnStageMachineSemantics`）。

**修掉的真实缺陷（D5）**：`design.md` §4.8 把 `executing`/`settled` 列为真实阶段，但生产代码**只有定义与读取、没有写入者** —— `applyTurnTakeover` 里针对二者的分支是死代码，「在 executing 边界崩溃」既不可达也不可测。更严重的是：一旦真的写入了 `executing`，恢复会走错误分支 → `FailTurnCognition("turn_stage_not_executable")` → **把一个完全可恢复的轮次永久隔离**。现在 `executing` 是真实可达且可恢复的：崩溃在飞行中被读取为「同裁决、不覆盖、继续跑幂等执行路径」。反向验证：把资格门改回只认 `winner_ready` → `TestRecoveryFromExecutingResumesWithoutANewVerdict` 精确失败（`turn_stage_not_executable`）。

**崩溃注入原语（`takeoverChainRewind`）与三个必须记住的坑**：
1. 必须删 `cognition_action_outcomes`：它按 `(action_id, call_id)` + `request_digest` 幂等，重放内容不同的结算会报 `action_outcome_replay_conflict`（另一条守卫，不是被测对象）。
2. 必须复用**完全相同的用户文本**：`enqueueTurnFactTx` 把「存档 text == 本次 text」当幂等校验，不同文本直接 `ErrConflict`，而错误文本是 `resource revision conflict`，完全不指向真因。已把三种文本提为常量。
3. 基线要选对：`a_frozen` 必须用**被拒（decline）轮**做基线。用接管（approve）轮会让 B 的 decision 冒充候选 A，且 scope 的 reply owner 变成 B → `takeover_rules.source_profile_id` 不匹配 → 规则选不中、Judge 零调用。

**断言要求**：恢复用例统一要求「**零物理 HTTP attempt**」——`router.requestCount(schema)` 只统计匹配到脚本的请求，所以要给该轮能触达的每个 schema 都注册脚本（哪怕返回 503），计数才有牙；新增 `totalRequests()` 兜底。

**失效快照（并发）**：回拨 `winner_ready` 后并发推进 `fluctlight_inner_states.revision` 与 `fluctlight_personality_runtime.active_profile_id='twilight'` → 结算被 `current_state_revision_stale` 拒绝，零 assistant 消息，持久 active 与 persona revision 均未被回写。（观察到：stale revision 被映射为落库码 `capability_settlement_failed` 而非 `life_context_stale`——既有精度问题，非本阶段范围，已记录未修。）

**M10 与隔离**：新增 `productionSourceFiles()` 扫描整个包的非测试源码，断言 `cognition_assessments` / `cognition_decision_proposals` **只有 INSERT、零 `FROM`/`JOIN` 消费者**；被拒候选在 `turn_takeover.go` 内只有 2 处写入（诊断记录 + payload 镜像），其它生产文件 0 引用。同时确认结构化轮次**结构上不可能**写入 appraisal/state revision：Provider 被禁止提供 `cognitive_state_transition`（`provider_cognitive_state_transition_forbidden`），`applyFrozenCognitiveStagesTx` 因此恒早退 —— 这让既有「无 A 痕迹」断言从「碰巧为真」变成「有结构性理由」。

**验证**：`turn_takeover_recovery_test.go` 10 例全 PASS；全量 `./internal/core/` → `ok 79.915s`（无回归）；`go build` / `go vet` 干净；`gofmt -l` 无输出。任务板 #35 / #36 / #37 / #38 关闭，`implement.md` 阶段 7 已勾选并附执行记录 + 回滚点 R7。

**仍未覆盖（顺延阶段 10）**：F10 的**有界重试策略**（本阶段只提供分离计数面与「零物理请求」断言，不宣称物理恰好一次）；§8 的运行时阶段/调用预算。

**状态**：阶段 7 完成并全绿。下一步 **阶段 8：基础缺陷复核与修复（R11、F14）**。

---

## 阶段 8：基础缺陷复核与修复（R11 / F14）— 完成

**范围**：7 条线索逐条复核，成立的 5 条各修一处 + 每处一条**精确字段测试**；1 条改为「尚未充分复核」；1 条只补窄断言（不改代码）。附加 F12 的注释/研究文档修正。

**代码改动（5 个生产文件）**：

- `app.go`
  - `normalizeInitializationAliases`：字符串 `role` 成为**首个** label 候选 → 保留原值（原先因缺 `type` 退化为 `"unknown"`）。
  - 新增 `initializationScopeProfileID(item, invalidCode, profileIDs)`：缺省 `profile_id` → **共享作用域（写 SQL NULL）**，显式值仍走 membership 校验。
  - `insertAgency` / `insertRelationshipSeeds` 去掉 `defaultProfileID` 形参；关系插入 `profile_id` 改 `nullableString(profileID)`。
  - 新增 `defaultFieldSource` / `usesDefaultUnit` / `recordInitializationDefaultFieldSources`：未声明的 personality/policy 键、目标 `importance`/`urgency`、意图 `confidence` 按稳定字段路径记入 `provenance.field_sources = "server_default"`；在 `CreateFluctlight` 里捕获 `declaredPersonality`/`declaredPolicy` 并在事务前调用。
- `goal_intention.go`：`GoalAuthority.Validate` / `IntentionAuthority.Validate` 允许空 `ProfileID`（= 共享作用域），不再把它当「缺身份」。
- `evolution_persistence.go`：goal/intention 的 insert+update 四处 `profile_id` 改 `nullableString(after.ProfileID)`。
- `working_persona.go`：Working Persona 的 `personality` 统一过 `normalizeEvolutionPersonalityBaseline`（与 evolution 层同一 reconciler），把扁平 trait **加法**补进规范 `traits.*`，随后删除空 `traits`/`expression` 载体。
- `provider_context.go`：`compactMessageTime` 由 `parsed.UTC().Format("01-02 15:04:05")` 改为 `parsed.Format("01-02 15:04:05Z07:00")`。
- `capability_runtime.go`：`settleDeferredCapabilitiesTx` 注释按 F12 重写（**在结算事务内**、deferred 只做本行写入 + intent 登记、**无网络 IO**、外部执行在提交后由 outbox/工作流完成、Preflight* 只读配置）。**未改**任何事务边界。

**新增测试（4 个文件 / 9 例）**：`initialization_scope_test.go`（role label 三条断言；goals/intentions/relationships 逐行 `profile_id` NULL vs 声明值；未声明 profile 仍被拒）、`initialization_defaults_test.go`（9 条字段路径逐条断言 + blank_slate 端到端读回 provenance）、`working_persona_test.go`（扁平→`traits.*` 加法镜像、空载体不外泄；散文 traits 逐字保留且不臆造数值）、`provider_context_test.go`（时区 `+08:00`/`-05:00`/`Z`；自身历史永不归属 `actor_user`）。

**两个刻意的「不修」**：
1. **assistant 自我举证**：不是代码缺陷。既有测试 `TestCompactRecentMessagesUsesActorUserAndFluctlightDisplayName` 把「自身消息用 fluctlight display_name」明确写成**设计意图**（F14.4 的教训：改盘点不改代码）。交付物改为窄断言 `TestRecentHistoryNeverAttributesSelfUtteranceToTheUser`——锁住「永不归属为 actor_user + 保留 actor_type=fluctlight 与时间戳」，使「曾经说过」始终可辨认为**发言**。
2. **TOON 递归**：research 里原先以「扁平渲染」推翻它，判据不充分（与 F14.1 同类错误），改为**「尚未充分复核」**如实标注，不声称成立或不成立。

**踩到的两个新坑**：
- **同一文件批量 Edit 会静默丢改动**：一条消息里对同一文件发多个 Edit，工具报成功但只有部分落盘（本轮在 `app.go` 上连丢 4 个 Edit，靠 read-back 才发现）。**同一文件一次只发一个 Edit 并回读确认**。
- **改格式字符串必须扫全部固定字面量断言**：时间戳格式改动后，全量测试挂 2 例（`provider_context_test.go:244/:271` 断言旧格式 `"09-03 00:00:00"`）。编译器不会发现字符串断言。

**验证**：全量 `./internal/core/` → **`ok 109.823s`**（首次跑挂 2 例旧时间戳断言，同步后全绿）；`go build` / `go vet` 干净；`gofmt -l` 无输出。任务板 #39–#43 关闭；`implement.md` 阶段 8 已勾选并附 **8.1 执行记录**（缺陷×改动×测试对照表 + 逐条复核结论）；`research/inventory-persona.md` 5 行结论同步为「已修/尚未充分复核」并记录修复手段；`research/inventory-turn-chain.md` 18e 行与 §6 末条统一为 F12 口径。

**仍未覆盖（顺延阶段 10/11）**：多 profile 记忆/关系的 A-only·B-only·shared **完整读写矩阵**（阶段 7/8 只验到 prompt 侧作用域、projection 重建与初始化写入作用域）；TOON 递归的递归调用链证据；F10 的有界重试策略与 §8 运行时预算。

**状态**：阶段 8 完成并全绿。下一步 **阶段 10：静态守卫、运行时预算与规范同步（§8、F10、F13）**（阶段 9 已取消）。

## 阶段 10：静态守卫、运行时预算与规范同步（§8、F10、F13、顺延 F04）✅

**1. 静态守卫重构为跨文件精确计数**（#44）：`TestCapabilityRuntimeStaticGuards…` 从单文件检查升级为全 `productionSourceFiles` 扫描——A/B 主生成各 1 处、Judge 仅 `turn_takeover.go` 恰 1 处、`mutations.go` 续调用恰 2、`not_proposed` 唯一出口、四流程文件禁具体能力分派、`rejected_candidate` 仅诊断、三结算点 0 处直调 `applyPersonalityDecisionPlanTx`、无递归仲裁。共享助手 `assertProductionOnlyIn`。反向验证：`composite_actions.go` 注入 `CapabilityName ==` → 变红。

**2. 运行时预算/阶段测试（F10）**（#45）：新增 `turn_chain_budget_test.go` 三例——阶段序列预算（三场景调用序列）、逻辑 vs 物理分离（`schemaSequence()` 保序、Judge 失败三形态各恰 1 次/0 次 B/`judge_degraded`+outcome、`unattributedRequests()==0`）、五阶段边界恢复不重演。反向验证：注入第二次 `judgeTurnTakeover` → 预算+守卫双红。中途修正一处旧断言（`takeoverChainFrozenPayload` 失败记录的 outcome 字段路径）。

**3. 多 profile 读写矩阵（F04，顺延项）**（#46）：新增 `takeover_scope_matrix_test.go`。播种 shared + A-only + B-only 的关系/目标/意图行 + 双视角记忆。① 读矩阵：A/B wire payload 各含「自有+shared」、互不含对方、自有关系遮蔽 shared、`current_profile_perspective` 跟随发言者；② 写矩阵 4 子场景：Judge 拒→A 行+1、Judge 准→B 行+1、无自有行→shared+1 不建行（依据 `recordRelationshipInteractionTx(frozenReplyOwner)` 的 profile 优先 SQL）；③ 下一轮恢复：Main 回 spark 视角、持久 active 仍 spark、两轮各写各行。**反向验证 ×3 全部有牙**：结算忽略 reply owner / `selectActiveProfileRelationships` 忽略 profile / `compactMemoriesForProfile` 丢 profile，注入均被抓，回退后 grep 确认无 "TEETH CHECK" 残留。

**4. F13 规范同步**（#48）：三份 inventory 的 superseded 标注（5/3/2 条，均带替代设计）早前已完成，本次核对无遗漏；`implement.jsonl`/`check.jsonl` 无 `_example` 行。新增三份 spec Scenario：`structured-turn-contract.md`（阶段机+预算+互斥+恢复）、`persona-layer-contract.md`（reply-owner 读写矩阵）、`fluctlight-cognitive-runtime.md`（结算权威+失败码）。架构守卫核对：13 能力不变、role 白名单无需登记（#47）。

**本阶段踩坑（新增两条）**：
- **INSERT 参数编号跳号会报 42P18**：多行 VALUES + 位置参数时，跳过 `$3` 或引用超出参数个数的 `$6`，Postgres 报 "could not determine data type of parameter"，且报错定位在**整条语句**而非具体行——按编号连续重排即可。
- **grep -r 扫 `apps/core-go` 根目录会被 SIGTERM（exit 137）**：目录下有海量二进制/缓存，只扫 `internal/`。

**验证**：`go -C apps/core-go test ./internal/core/` 全绿 **`ok 113.902s`**（含全部新增测试）；`gofmt -l` 无输出；`go vet` 干净。任务板 #44–#48 全关闭；`implement.md` 阶段 10 勾选完毕并附执行记录。

**状态**：**阶段 10 完成（任务板全部关闭）**。剩余：阶段 11（验证与基准，R12/§17/§18）→ 阶段 12（清理与交付物）。TOON 递归证据仍标记「尚未充分复核」。

## 阶段 11：验证与基准（R12、§17、§18）✅

**1. 11.1 确定性场景**：三场景已有覆盖（pure QUERY / 混合批次 / B 持久切换提案）。补端到端缺口 `TestAuthorizedPersistentSwitchLoadsTheNewPersonaNextTurn`（`phase11_verification_test.go`）：授权持久切换 → 结算写 active → 下一轮 wire 加载新人格（TWILIGHT marker 在、SPARK marker 不在）。反向验证：E3 门改空操作 → 红。

**2. 11.2 精确字段契约**：9 项断言中 7 项已有测试映射；补两个缺口——`TestProviderRosterCarriesOnlyIdentifierFields`（roster 键 ⊆ {id,name,profile_id}，反向验证放行全部键 → 抓到 personality 泄漏）、`TestWireBudgetAccountsForToolsAndResponseFormat`（SectionTokens 与 EstimatePromptTokens 精确相等、EstimatedInputTokens 增量精确）。

**3. 11.4/11.5 成本测量**：`turn_path_cost_report_test.go` 四路径全 wire 测量 + 预算不变量断言，产出 `testdata/turn_path_cost_report.json`。结果：plain 1 请求 23.5k tok / judge_keeps_a 2 请求 25.3k / judge_takeover_b 3 请求 48.3k（≈2.06×plain，含被丢弃 A 输出）/ pure_query 2 请求 30.4k。Judge 仅 ~1.8k tok（主请求 9%）。

**4. 11.6 如实标注**：F14.2 抗注入结论不可用（live provider 未运行）；多头卡接管语义产出未验证；deterministic 求值未实现；输出侧均为脚本值。均写入 implement.md 11.7 执行记录。

**踩坑**：`EstimatePromptTokens(nil)` 与 `EstimatePromptTokens([]map[string]any{})` 不同（5 vs 3）——typed nil slice 走 json.Marshal("null")，断言空节成本时用空切片基线。

**验证**：全量回归绿；`go vet`/`gofmt` 干净。阶段 11 完成；下一阶段为阶段 12（清理与交付物：旧逻辑清理、§20 八问、架构文档、迁移表核对）。

## 阶段 12：清理与交付物（§19、§20、R12）✅ —— 任务关闭

**清理核对**：未授权 schema 无 personality_decision、takeover_rules 不进 Main、profiles {id,name}、授权入口保留——全部映射到既有测试。单一执行链确认：零 feature flag（生产代码无 os.Getenv 开关）、能力注册表恒 13、静态守卫禁具体能力分派。迁移全为 CREATE TABLE IF NOT EXISTS（零 DROP/DELETE），阶段状态存既有 JSONB。

**交付物**：`docs/persona-takeover-architecture.md`（架构摘要：链路/权威模型/作用域/预算/规则体系/回滚）+ `docs/persona-takeover-delivery-report.md`（文件清单、运行方式、清理情况、规则迁移表实际数据核对、测试与性能摘要、§20 八问逐项回答）。代码规模：24 文件修改 +1088/−275，6 个新生产文件，16 个新测试文件。

**多头卡结论**：dense_multi_card 接管语义在归一化层验证可产出可执行规则（规则身份不随措辞漂移）；live provider 端到端未运行，如实标注、未放宽 fixture。

**遗留**（环境阻塞或已决议不做）：live provider 测试（F14.2 抗注入、多头卡端到端）、deterministic 时间窗求值器（选项 A）、TOON 递归证据（未充分复核）。

**任务状态：09-14-persona-takeover-working-prompt 阶段 5–8、10–12 全部完成，G0–G2 门全过，任务关闭。**

## 收尾核对：implement.md 勾选账目补齐

用户问「全都完成了吗」时发现 implement.md 还有 35 个未勾选项——全部属于阶段 1–4（本会话之前完成的阶段，任务板 #1–#28 已关闭），属勾选遗漏而非未完成工作。已核对生产代码确认交付物齐全（persona_switch_rules.go / persistent_switch_gate.go / working_persona.go / visible_output.go 等含全部 E1–E5 与归一化函数），用 perl 批量补勾（sed -i '' 在此环境报 "No such file or directory"，zsh 兼容问题）。唯一保留未勾：`strict_visible_text_reconciliation` 可选加强项（评审决议默认不启用）。

**最终状态：任务 09-14-persona-takeover-working-prompt 全部完成并关闭——任务板 49/49、implement.md 仅剩 1 项决议性未勾、全量回归绿。**

## 验收报告整改（final-acceptance-report.md F-01~F-06）— 进行中

用户要求查看 final-acceptance-report.md 并修正。核实报告 F-01~F-06 全部代码事实准确。用户决策：F-01 保留根字段+修订契约；F-03 完全移除关键词分类；6 项全做。

**F-03 移除生产自然语言语义分类 ✅**：
- 删 personaTakeoverMarkers/personaPersistentMarkers/personaTimeWindowPattern/personaTimeWindowKeys/personaContainsMarker/classifyForcedActivationKind/classifyPersistentRuleKind/personaRuleCarriesTimeWindow/personaKeysWithValue（死代码）。
- 新增 declaredSwitchRuleKind：只读显式 `kind` 声明，校验合法枚举。
- appendForcedActivationCandidates：prose 无声明 kind → unclassified + 诊断；无 target 无 named → unclassified 诊断 continue。
- appendPersistentCandidate：声明 kind 优先，默认 persistent_semantic，deterministic 只能显式声明。
- 保留 personaTextNamesProfile/isASCIIText（profile 名是声明标识符，匹配算结构解析）。
- 测试：结构化声明加显式 kind；真实多头卡 prose 改期望 unclassified+诊断。
- 架构守卫 persona_switch_rules_f03_guard_test.go：禁 forbidden 符号回归+marker slice，反向验证有牙。

**F-04 恢复 pending takeover 按冻结规则 ✅**：
- turnTakeoverRecord 加 RuleCondition 字段；applyTurnTakeover base 存 rule.Condition。
- 新增 resumeTakeoverRuleFromPayload：从冻结 payload 读 rule_id/digest/target/condition/version，验证 target 仍声明+version 匹配，不调 selectTurnTakeoverRule。
- resumeArbitration pending 分支改用 resumeTakeoverRuleFromPayload。
- 测试 turn_takeover_f04_test.go 两例：ghost-target fail_closed（rewrite 冻结 target 为未声明 profile → resumeTakeoverRuleFromPayload 验证失败；反向验证重选版会成功→有牙）+ 正常恢复保留冻结 rule_id/target/condition。

**踩坑**：(1) go test 缓存——不加 -count=1 时改代码后仍可能命中缓存致假绿，反向验证必须 -count=1。(2) "移除 takeover_rules 后 input.Switch 仍含规则"——恢复路径 personaSwitch 用入口 projection 但 hydrated.Projection 替换 projection，实际重选仍能选到；故 F-04 测试改用 rewrite 冻结 target 为 ghost-profile 才能区分重选 vs 冻结恢复。

**剩余**：F-05（Judge control view 合并 B overlay）、F-06（life_profile 裁剪）、F-02（Judge 前权限校验）、F-01（回复权威契约）、复验。

**F-05 Judge control view 合并 B overlay ✅**：
- `working_persona.go:107-125`：overlay 应用条件从 `subject==activeProfileID` 扩展为 `subject==activeProfileID || EffectivePersona.profile_id==subject`，使 rescope 后的 B overlay 能应用到非激活 B subject。
- `takeover_scope.go:100-103` `rescopeEffectivePersona`：EffectivePersona map 新增显式 `"profile_id": owner` 字段（profile_ref 是 ref 字符串非 profileID，不能直接比对）。
- `turn_takeover.go`：新增 `(*App).projectTakeoverControlView(ctx, rule, projection)`——对非激活 target 先 `contextProjectionFromValue` 深拷贝 + `rescopeEffectivePersona` 为 B 组合 overlay，再 `buildTakeoverControlView`；只读 rescope（不改 active_profile_id/reply owner/reference index），失败回退 baseline view。`judgeTurnTakeover` 的 `ControlView` 改用它。
- 测试：`working_persona_test.go` 两个单元测试（rescope overlay 应用到非激活 subject + 无 profile_id 的 legacy overlay 不应用）；`turn_takeover_f05_test.go` 两个集成测试（DB seed twilight active overlay → control view stance openness=0.55 且 reply owner 仍 spark；无 overlay → baseline 0.3）。
- 反向验证：禁用 profile_id 闸门后两测试均 FAIL（得 baseline 值），恢复后绿。全量回归 125.4s 全绿（core + httpapi + migrations + platform + workflow + config）。
- 踩坑：(1) `profile_ref` 是 `"personality:ctx_"+hash` ref 字符串，非 profileID，故条件改用显式 `profile_id` 字段而非 `profile_ref==subject`。(2) `normalizeEvolutionPersonalityBaseline` 保留 flat 顶层 baseline + nested overlay（canonicalization 设计，TestWorkingPersonaCanonicalizesFlatTraitsShape 既定），故"0.3 不泄露"断言过度，改为断言 traits.openness=0.55 + 序列化含 0.55。(3) DB overlay insert 须满足 `ck_evolution_overlay_authority_v1`：ref 须 `evolution_overlay:ctx_[a-f0-9]{32}`、revision=base_revision+1、cooldown_until NOT NULL、before/after_value 用参数化 float64 入 jsonb number。(4) 测试 projection 须给 profile 声明 behavioral_policy，否则 `normalizeEvolutionBehaviorBaseline(nil)` panic（生产 projection 从 DB 读必有值，非 F-05 引入）。

**剩余**：F-06（life_profile 裁剪）、F-02（Judge 前权限校验）、F-01（回复权威契约）、复验。

**F-06 按任务裁剪 shared life_profile + 前后对照 ✅**：
- 调研依据：`fluctlight-life-world-contract.md:28-35` life_profile 含 8 键（appearance/social_background/preferences/life_habits/recurring_commitments/relationship_seeds/character_constraints/media_preferences）；`request.md:216`「完整背景故事、视觉细节、全部习惯...不默认每轮全量注入」；`design.md:401`「不进入投影：完整背景故事、全部经历、视觉资产细节」。
- `working_persona.go`：新增 `workingPersonaLifeProfileKeys` allowlist（仅 `preferences` + `character_constraints`）与 `filterWorkingPersonaLifeProfile`（只保留决策键 + 对每个保留键复用 `filterCorePersonaValue` 去元数据）；`shared.life_profile` 注入从 `filterCorePersonaValue(lifeProfile)` 改为 `filterWorkingPersonaLifeProfile(lifeProfile)`。appearance/social_background/life_habits/recurring_commitments/relationship_seeds/media_preferences 延后到各自 task-scoped 投影（visual_identity/schedule/relationship/media capability）。
- 测试（`working_persona_test.go`）：`TestWorkingPersonaLifeProfileKeepsOnlyDecisionKeys`（dense 8 键 fixture → 只保留 2 决策键，6 大段键不泄露，preferences/character_constraints 值保留）；`TestWorkingPersonaLifeProfileBeforeAfterComparison`（生成 `testdata/life_profile_before_after_report.json` 机器可读对照：before 8 键/753B life_profile/990B working → after 2 键/174B life_profile/432B working，life_profile -77%、working -56%）。
- 反向验证：禁用 allowlist 检查后 `TestWorkingPersonaLifeProfileKeepsOnlyDecisionKeys` FAIL（8 键全泄露），恢复后绿。现有 working persona / provider persona 回归全绿（multiProfile fixture life_profile 只含 preferences，裁剪后行为不变）。
- 范围边界：F-06 仅裁 `life_profile`；`identity.background_story` 等背景故事在 identity 层（非 life_profile），验收报告 F-06 未覆盖，留作后续；`secrets` 全文按 design.md:401 同属不进入但已有独立处理（working_persona.go:31-38 注释保留 decision inputs）。

**剩余**：F-02（Judge 前权限校验）、F-01（回复权威契约）、复验。

**F-02 Judge 前权限与冻结上下文约束校验 ✅**：
- 新增 `candidateValidationContext` + `validateCandidateCapabilityInvocations`（capability_runtime.go）：先跑 `validateCapabilityInvocationsForPersistence`（registry+schema），再做确定性授权闸门——声明 surface（`definition.Surfaces` 含 invocation surface）、冻结身份所有权（`Metadata.FluctlightID`/`ConversationID` 非空时必须等于候选）、声明 target kinds（`OutputBinding.TargetKind` 在 `definition.TargetKinds` 内）。无副作用、无 DB/context-resolver I/O；网络/可重试 Preflight 留 winner-only Prepare。
- A 路径（mutations.go:807）+ B 路径（turn_takeover.go:913）改用新方法，传入候选 FluctlightID/ConversationID/Conversation surface。
- 静态守卫（turn_takeover_chain_test.go:876/909）更新匹配新方法名。
- 测试：`TestValidateCandidateCapabilityInvocationsAuthorizesDeterministically`（单元 surface/fluctlight/conversation/targetkinds 未授权→ErrUnauthorized）、`TestUnauthorizedCandidateAFailsBeforeTheJudge`（A 调 surface 不匹配 spy→Judge 0 调用、turn 失败、无消息）、`TestUnauthorizedTakeoverCandidateBFailsControlled`（B 调未授权 spy→B 失败、turn 受控失败、A 未送达）。反向验证有牙（禁用 surface 闸门→两测试 FAIL）。
- 踩坑：spy 用 `unauthorizedSurfaceSpyCapability`（Surfaces=[WakeUp] 不含 conversation）；注入 app 用 `appWithExtraCapabilities`（rebuild registry/runtime）；`resolveCapabilityAction` 对不在 catalog 的 spy `continue` 返回 no_op（不报错，到 validate 抓 surface）。

**F-01 统一回复提案权威（适度 path b）✅**：
- 用户决策"保留根字段+修订契约"。初版严格 path b（reply 不是提案源+reply-only 拒绝）破坏 3 个生产测试（reply-only 是 request.md:13「回复已经是 Tool」原契约合法形态）。调整为**适度 path b**：根 `visible_text`（response_plan/decision）是优先正式协议，`conversation.reply` 仍是 reply-only fallback 提案源（Core 从 reply.text 派生 canonical）；但根字段与 reply.text 不一致 → `visible_text_source_conflict` **fail closed**（`normalizeTurnDecision` 返回错误，Turn 受控失败），不再静默选 winner。避免模型同时决定两份文本。
- `resolveCanonicalVisibleReply`（visible_output.go）：保留三源 switch（含 reply fallback）+ 冲突诊断（detail 更新）；移除初版 reply-only 诊断块 + 未用常量 `canonicalVisibleReplyOnlyWithoutRootCode`。`turn_decision.go`：conflict fail closed（`if canonical.Conflict { return error }`）。
- 契约修订：design.md §4.3（适度 path b：根优先+reply fallback+冲突 fail closed+reply-only 合法）、implement.md:204（strict_visible_text_reconciliation 已启用）、request.md:13 path b 加注。
- 测试：`TestResolveCanonicalVisibleReplyFallsBackToReplyCapability`（reply-only 通过，原契约保留）、`TestConflictingVisibleTextSourcesFailClosed`（三源冲突→turn 失败、无消息）、`TestConflictingReplyArgumentFailsClosed`（根+reply 不一致→fail closed、Judge 0 调用）、`TestRootVisibleTextWithoutReplyInvocationIsAccepted`（无 reply 有根字段→消息从根字段送达）。反向验证有牙（禁用 conflict 闸门→测试 FAIL）。

**复验 #61 进行中**：gofmt -l 无输出、go vet ./internal/core/ 干净、go build ./... 通过、go test -count=1 ./... 全绿（core 146.3s）。`go test -race -count=1 ./...` 后台跑（task K4KPae），结果待补。真实模型验收（#8）因隔离 Provider 环境缺失保持「未验证」，SKIP 未计为通过。验收报告 §12 整改标注待 race 完成后写入。
