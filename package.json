{
  "name": "fluctlight-workspace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.0.7",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter @fluctlight/bff dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "generate": "pnpm --filter @fluctlight/core-client generate && pnpm --filter @fluctlight/bff generate:browser-client && pnpm --filter @fluctlight/browser-client generate"
  }
}
